import  random
class Arrays():
    array=[]

    @staticmethod
    def fa(len)
    for i in range(len):
        array.append()

    @staticmethod
    def fb(lenght,x,y):
        for z in range(lenght):
            Arrays.array.append(random.randrange(x,y))
        return Arrays.array

a=Arrays.fb(3,6,9)
print(a)

    
